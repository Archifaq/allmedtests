---
import { t, type LocaleCode } from '../i18n';

interface Props {
  locale?: LocaleCode;
}

const { locale = 'en' } = Astro.props;
---

<header>
  <a href="/" aria-label={t('site.homeAriaLabel', locale)}>{t('site.brand', locale)}</a>
</header>
