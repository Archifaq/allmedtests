---
import { t, type LocaleCode } from '../i18n';

interface Props {
  locale?: LocaleCode;
}

const { locale = 'en' } = Astro.props;
const affiliateDisclosure = `\n${t('footer.affiliateDisclosure', locale)}\n`;
---

<footer>
  <p>{affiliateDisclosure}</p>
</footer>
